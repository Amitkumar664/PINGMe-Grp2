const { createLovableConfig } = require("lovable-agent-playwright-config/config");

module.exports = createLovableConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
